<div class="row">
  <div class="col-sm-4">
    <%= render "shared/account_navbar" %>
  </div>
  <div class="col-sm-8">
    <div class="card">
      <div class="card-body">
        <h2><%= @team.name %></h2>
        <%= link_to "Edit Team", edit_team_path(@team), class: "btn btn-primary" %>

        <%#= render @teams %>
      </div>
    </div>
  </div>
</div>
