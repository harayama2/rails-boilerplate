<div class="card my-4">
  <div class="card-body">
    <div class="d-flex justify-content-between align-items-center">
      <h5><%= team.name %></h5>
      <%= button_to "Switch", switch_team_path(team), method: :patch, class: "btn btn-secondary" %>
    </div>
  </div>
</div>
